# Rust
/target
**/*.rs.bk

# Node / TypeScript
node_modules/
dist/
*.tsbuildinfo

# WASM build output (generated by `pnpm run build:wasm`; the .wasm + wasm-bindgen glue are
# reproducible build artifacts — the script + crate are committed, the output is not).
pkg/
*.wasm
packages/sdk/vendor/allw-wasm/

# Cloudflare / Wrangler
.wrangler/

# Env
.env
.env.*
!.env.example

# Local & OS
*.local.*
.DS_Store
scratch/
.worktrees/

# Claude Code local configuration (auto-added)
.claude/*.local.*
