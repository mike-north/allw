{
  "name": "@allw/relay",
  "version": "0.0.0",
  "description": "Zero-knowledge approval relay: device pairing, ciphertext routing, push fan-out (Cloudflare Workers + Durable Objects).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "build": "echo \"relay builds via wrangler\" && exit 0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.14",
    "@cloudflare/workers-types": "^4.20260609.1",
    "@types/node": "^22.0.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.0.0"
  },
  "license": "MIT"
}
